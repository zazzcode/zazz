# Deliverables

This directory is reserved for deliverable-level SPECs and PLANs.

Default policy for this repo:

- treat deliverable docs as worktree-local execution artifacts
- commit them only when they are intentionally being shared as durable review context
- keep long-lived product intent in `docs/proposals/`, `docs/features/`, and `docs/standards/`
